export type ItemType = {
    code: number;
    name: string;
    description: string;
    pic: string;
};

export type ItemTypeList = ItemType[];

export type heroType = {
    header: string;
    subhead: string;
};

export type HeroTypeList = heroType[];

